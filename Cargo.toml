[package]
name = "ioix"
version = "1.0.0"
edition = "2024"
description = "A lightweight CLI tool for managing project metadata using JSON"
license = "EPL-2.0"
repository = "https://github.com/usernullex001/ioix"
readme = "README.md"
keywords = ["cli", "metadata", "json", "project"]
categories = ["command-line-utilities"]

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
serde = {"version"="1.0.229","features"=["derive"]}
serde_json = "1.0.151"
[build-dependencies]
serde = {"version"="1.0.229","features"=["derive"]}
serde_json = "1.0.151"
